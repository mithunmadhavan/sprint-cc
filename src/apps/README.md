# Apps Microservices

This workspace uses app-level services under `src/apps/`:

- `src/apps/ey-sprint-business-services` - NestJS backend microservice.
- `src/apps/ey-sprint-frontend` - frontend static service.

Legacy root runtime files have been removed. `src/apps/*` is now the only runtime path.
